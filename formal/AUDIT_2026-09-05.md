# Adversarial audit of the Lean certificate — 2026-09-05

**Target.** `Av12453.TwoThreshold.av12453_count_kernel : ∀ n, G (n, 0) = (avoiders n (beta 2)).card`
in `formal/Av12453`, claimed to certify the counting algorithm of
`paper/av12453_polytime.tex` §§2–6 (`eq:K`, `eq:D`, `eq:G`, `eq:answer-G`).

**Method.** Synthesis of four independent lens audits (trusted base, faithfulness-to-paper,
soundness hygiene, structure), with every finding re-verified from the Lean source, the
`.tex`, and fresh Lean/Python runs. The prior `REPORT_*.md` files were not trusted. Nothing
in the repository was modified (`git status --porcelain` empty).

**Verdict: solid, with scope caveats.** No BLOCKER, no MAJOR. The theorem is unconditional,
axiom-clean, non-circular, and means what it says. Every confirmed finding is about *claim
calibration and process durability*, not about the mathematics or the proof.

---

## 1. Confirmed findings

### MINOR 1 — `formal/README.md:14` overstates what is certified: the quotient algorithm and the published data are outside it

> "The `d = 2` kernel theorem is the correctness statement of the paper's polynomial-time
> algorithm for `12453`."

What is certified is the **unreduced** `d = 2` kernel, `eq:K`/`eq:D`/`eq:G`/`eq:answer-G`.
The paper itself calls that specialization `O(N^8)` time and `O(N^5)` storage
(`av12453_polytime.tex:222-226`); the advertised `O(N^7)`/`O(N^4)` for `Av(12453)` comes
from the two translation quotients of `sec:d2-support` — `lem:d2-exact-support` (tex:1681),
`lem:d2-translation` (tex:1706), `eq:R-recurrence` (tex:1766),
`lem:d2-second-translation` (tex:1792), `cor:d2-complexity` (tex:1874). **None of these is
in the Lean development.** I confirmed this two ways: grep for translation/quotient/`R_`
content in the twenty modules returns nothing, and a body-level transitive-closure walk of
`av12453_count_kernel` (below) touches no such constant. The only `sec:d2-support` fragment
present is `KernelFactor.K_layer_zero` (`KernelFactor.lean:368`).

Consequence worth stating plainly: `prop:exact-150` — the shipped
`code/data/av12453_terms_0_150.txt` and `_0_300.txt` — was produced by
`av12453_fast_rns.cpp`, which "implements `\eqref{eq:R-recurrence}` and the empty-stack
recurrence `\eqref{eq:G}`" (tex:1934-1938). **The Lean theorem does not certify the code
path that generated the published coefficients.**

`README.md:62` already discloses the gap ("what is deliberately not formalized (complexity,
`thm:protected-tail-principle`, `sec:quotients`, general `d`)"); line 14 contradicts it.

*Fix:* soften line 14 to "the correctness statement of the unreduced `d = 2` kernel
recurrence (`eq:K`/`eq:D`/`eq:G`); the `O(N^7)` quotient of `sec:d2-support` and the
complexity bounds are not formalized."

### MINOR 2 — no complexity is formalized, and the Lean `K`/`G` are not the dynamic program

There is no cost model, memo table, or evaluation-order object anywhere in the twenty
modules. `K` and `G` are fuel-driven recursions that re-evaluate every subcall
(`TwoThreshold/Kernel.lean:139` `Kaux`, `:201` `K`, `:275` `Gaux`, `:320` `G`), not the
memoized sparse-row DP of `sec:algorithm` (tex:1594-1607).

This is not theoretical: dumping Lean's `K ℓ p t` for `ℓ ≤ 4`, `‖p‖₁ ≤ 4`, `‖t‖₁ ≤ 6`
together with `G p` for `‖p‖₁ ≤ 8` **exceeded a 600 s timeout in the Lean interpreter**,
while my Python transcription of the same recurrence with memoization returns the same
values in well under a second. The Lean object is the right *function*; it is emphatically
not the *algorithm* whose cost the paper analyses.

What *is* machine-checked from `sec:algorithm` is its mathematical content: `lem:support`
(`KernelSupport.lean:135` `K_eq_zero_of_mass_lt`, `:191` `K_support`) and the fact that
grade `w = ‖p‖₁ + ℓ` and mass `m` are well-founded orders (the termination measures of
`Kaux`/`Gaux` and of the strong induction in `KernelCount.lean:123` `G_eq_H_aux`).

*Fix:* one sentence in `README.md`; already correct in `REPORT_component_4b.md §9.4`.

### MINOR 3 — `example` declarations are invisible to the axiom sweep, contradicting the documentation

`Av12453/Axioms.lean:20-21` and `formal/README.md:74-75` both claim the sweep covers
"`private` declarations, `example`s and auto-generated constants". **The `example`s are not
covered.** Verified directly in the environment: the sources contain **112** `example`
declarations, but there are **0** constants whose name mentions `_example`, and
``env.find? `_example`` is `false` — Lean elaborates `example`s under `withoutModifyingEnv`,
so they leave no constant behind.

This cannot make the headline theorem false (nothing can depend on a constant that does not
exist). But it is a real coverage gap, because this development uses `example`s for two
audit functions a referee would assume are guarded:

* the **frozen-interface statement-drift checks** — `TwoThreshold/KernelCount.lean:176`,
  `:206`, `:287`, `:291`, `:296`, `:301`, `:304` and `TwoThreshold/KernelFactor.lean:381`,
  `:385`, `:389`, `:391`;
* roughly thirty numeric `by decide` cross-checks (e.g. `KernelCount.lean:241`, `:246`,
  `KernelFactor.lean:396`, `:407`-`:425`).

A `sorry` in any of these would emit only a build warning and pass the sweep silently.

*Fix:* correct the two doc claims, and/or convert the frozen-interface checks from
`example` to named `private theorem`s so the sweep reaches them.

### MINOR 4 — nothing automated enforces sorry-freeness

Verified: `lake env lean` on a file containing `theorem t : False := by sorry` and
`example : False := by sorry` emits two `warning: declaration uses 'sorry'` and **exits 0**.
`.github/workflows/lean_action_ci.yml` runs only `leanprover/lean-action@v1` (i.e.
`lake build`) plus `docgen-action`, with no fail-on-warnings setting, and **never invokes**
`lake env lean Av12453/Axioms.lean`. `Axioms.lean` is deliberately outside the
`lean_lib Av12453` glob and has no `.olean`, so CI never elaborates it.

`README.md:73` states "`lake build` must finish with no errors and no warnings" — a human
convention, not an enforced one. The whole no-`sorry` guarantee rests on a manual step. Fine
for this snapshot (I ran it: exit 0, `checked 1438 declarations`, zero `sorryAx`), but a
future edit introducing a `sorry` would ship green.

*Fix:* add `lake env lean Av12453/Axioms.lean` as a CI step.

### MINOR 5 — every source header cites a LICENSE file that does not exist

All twenty modules plus `Axioms.lean` carry "Released under Apache 2.0 license as described
in the file LICENSE". `find . -maxdepth 3 -iname 'LICENSE*'` at the repository root returned, at the time of the audit,
nothing, and no LICENSE is tracked under `formal/`. (Update 2026-09-14: `LICENSE`, Apache-2.0, now sits at the repository root.) The project task list already recorded "choose a
software license" as an outstanding submission task; the supplement currently asserts a
licence it does not ship.

### MINOR 6 — `REPORT_component_4b.md §9.2` misdirects the referee: `Trigger.lean` and `FirstLetter.lean` are not in the chain

§9.2 presents a "deliberately linear" dependency chain, "each item uses only the ones above
it", and lists as item 3 `Av12453/Trigger.lean` and `FirstLetter.lean`, calling them "the
two combinatorial inputs of the scan".

I computed the body-level transitive closure of `av12453_count_kernel` in Lean and counted
per-module contributions:

```
Av12453.Trigger:      0 of 20 constants in the headline closure
Av12453.FirstLetter:  0 of 34 constants in the headline closure
```

Cross-checked by grep: none of `trigger_lemma`, `trigger_lemma_two`,
`avoids_beta_iff_forall_trigger`, `contains_beta_of_trigger`,
`exists_trigger_of_contains_beta`, `avoids_231_cons_iff`, `belowBeforeAbove_iff_append`,
`length_filter_of_localRank` is referenced anywhere outside its own file. Both modules are
dead weight with respect to *both* certified chains — the `d = 2` scan re-derives what it
needs inside `TwoThreshold/Thresholds.lean`.

They are still valuable: they are standalone, axiom-clean certifications of the paper's
`lem:trigger` and first-letter lemma. But a referee told they are load-bearing links will
draw false confidence about the chain's coverage.

*Fix:* move them out of the numbered chain in §9.2 into a "additional certified paper
lemmas, not used by the chain" note.

### MINOR 7 (paper prose, not Lean) — `sec:algorithm`'s kernel-phase paragraph drops the `a,b ≥ 1` side condition

`av12453_polytime.tex:1596-1600`: "In each split term, for every $a+b=\ell-1$ and
$\mathbf u\in\supp K_a(\mathbf p,\cdot)$, add …" — versus `eq:K` at tex:1502-1504, which
carries $\sum_{a,b\geq1,\;a+b=\ell-1}$.

I confirmed this is materially consequential, not pedantry. Implementing the paragraph
literally (allowing `a = 0` or `b = 0`, where `K_0` is the identity kernel, thereby
double-counting `D_ℓ`) gives

```
with    a,b >= 1  (eq:K):        1, 1, 2, 6, 24, 119, 694, 4581, 33286   (correct)
without a,b >= 1  (algo para):   1, 1, 2, 7, 38, 272, 2300, 21725, 222630
```

— wrong from `n = 3`. `eq:K` itself is unambiguous and Lean is correct
(`Finset.Ico 1 (ℓ - 1)`, `Kernel.lean:233-241`). Repeating "with $a,b\geq1$" in the
kernel-phase paragraph closes it.

---

## 2. Notes (no action required, recorded so nothing is mistaken for a claim)

* **NOTE A — "Lean `K` = the paper's `K_ℓ(p,t)`" is a by-eye step, not a theorem.** The
  paper defines `K_ℓ^L(p,t)` *semantically* as a stopped-path weight (tex:1449-1475) and
  derives `eq:K` via `prop:kernel-recurrence` and the unformalized
  `thm:protected-tail-principle`. Lean **inverts** this: it takes `eq:K`/`eq:D`/`eq:G` as
  *definitions* and proves the consequence that matters. This is the safe direction and
  leaves no gap in the headline theorem, whose statement mentions neither `K` nor path
  semantics and whose chain is Lean-`K` → `H_factor` → `G_eq_H` → `av12453_count` →
  `avoiders`. But the identification with the paper's object rests on transcription
  fidelity, which a referee must check by eye (see §3) — I checked it line by line and
  reproduced it numerically (§4).
* **NOTE B — the truncated control sums are not merely harmless, they are necessary.**
  `eq:K` and `eq:G` sum over all controls; Lean sums over `S p = ctrls ‖p‖₁`
  (`Kernel.lean:119`, `:146`, `:279`). With an unrestricted `u`, the split term's second
  factor `K_b(u,t)` has unbounded grade `‖u‖₁ + b`, so **`eq:K` read literally is not a
  well-founded recursion until `lem:support` is available**. Lean's order — truncate,
  define, prove `K_eq_zero_of_mass_lt`/`K_support` about the defined `K`, then restate the
  equations over `ctrls M` for arbitrary `M ≥ ‖p‖₁` (`KernelSupport.lean:317`, `:330`,
  `KernelFactor.lean:204`) — is the only sound one, and is not circular. I confirmed
  numerically that an untruncated computation over a mass-14 box gives identical values and
  zero `eq:support` violations.
* **NOTE C — `D 0 p t` is a junk value.** `Kernel.lean:212-214`; with ℕ subtraction,
  `D 0 p t = 2 * K 0 p t = 2·1_{p=t}`, outside `eq:D`'s domain (tex:1508-1512, `ℓ ≥ 1`).
  Confirmed inert: `D` is reached only through `D_one`, `D_succ` (`2 ≤ ℓ`) and inside
  `K_succ_eq` (`1 ≤ ℓ`); no application at `ℓ = 0` exists anywhere.
* **NOTE D — `Av12453.OrderIso` shadows Mathlib's `OrderIso`** (`Basic.lean:138`). Inside
  `namespace Av12453` an unqualified `OrderIso` is the list relation, not `≃o`. Readability
  hazard only; the printed theorem statement is fully qualified and all 1438 project
  constants are rooted at `Av12453`.
* **NOTE E — `Contains w [] ` is true for every `w`, so `avoiders n [] = ∅`** (confirmed:
  `#eval` gives `[0,0,0,0,0,0]`). Standard convention; no bearing on `beta 2` (length 5).
* **NOTE F — six of the 24 `#eval` blocks are unguarded by `#guard_msgs`**
  (`OneThreshold/Kernel.lean` ×2, `OneThreshold/Counting.lean` ×1,
  `TwoThreshold/Kernel.lean` ×1, `KernelCount.lean` ×1, `KernelFactor.lean` ×1); one prints
  on every build. `#eval` is compiler-evaluated, not kernel-checked, and no theorem depends
  on any of them. The 18 guarded ones do fail the build if a value changes.
* **NOTE G — the manuscript never mentions the certificate.** `grep -i
  "lean|formaliz|machine-check|certificate"` over `av12453_polytime.tex` returns **nothing**.
  As things stand the Lean development carries no weight for the submission; if it is meant
  to support the paper it must be cited and deposited with the supplement.
* **NOTE H — correction to a prior audit report.** The soundness-hygiene audit claimed an
  "independent cache-free deep axiom walk … traversing `(env.setExporting false).checked.get`"
  via `ConstantInfo.value?`. **That method does not work on this toolchain.** I verified
  that `value?` returns `none` for imported theorems in the default, non-exporting *and*
  kernel environments, so such a walk silently visits only *types*: my faithful
  reproduction of it visited 725 constants and found **zero** axioms — not the claimed
  three. The walk must match `.thmInfo v => v.value` explicitly. Done correctly (§4), the
  conclusion stands, but it stands on the corrected method, not the reported one.

---

## 3. The trusted base a referee must check by eye

Everything below appears in, or is definitionally reachable from, the statement of
`av12453_count_kernel`. Nothing else needs to be read to know what the theorem *says*.

**Statement side — "is this really `|Av_n(12453)|`?"**

| file:line | definition |
|---|---|
| `Av12453/Basic.lean:138` | `OrderIso u v := u.length = v.length ∧ ∀ i j …, u[i] < u[j] ↔ v[i] < v[j]` |
| `Av12453/Basic.lean:404` | `Contains w τ := ∃ s, s <+ w ∧ OrderIso s τ` (`<+` = `List.Sublist`) |
| `Av12453/Basic.lean:407` | `Avoids w τ := ¬ Contains w τ` |
| `Av12453/Basic.lean:123` | `IsPermOf n w := w.Perm (List.range n)` |
| `Av12453/Basic.lean:650,653` | `iota d := List.range d`; `beta d := iota d ++ [d+1, d+2, d]` |
| `Av12453/OneThreshold/Defs.lean:518` | `perms n := (List.range n).permutations.toFinset` |
| `Av12453/OneThreshold/Defs.lean:524` | `avoiders n τ := (perms n).filter fun w => Avoids w τ` |

**Algorithm side — "is this really `eq:K`/`eq:D`/`eq:G`?"**

| file:line | definition |
|---|---|
| `Av12453/TwoThreshold/Kernel.lean:102,119` | `ctrls M`, `S p := ctrls (p.1 + p.2)` |
| `Av12453/TwoThreshold/Kernel.lean:139` | `private def Kaux` — the fuel form of `eq:K` |
| `Av12453/TwoThreshold/Kernel.lean:201` | `K ℓ p t := Kaux (p.1 + p.2 + ℓ + 1) ℓ p t` |
| `Av12453/TwoThreshold/Kernel.lean:212` | `D ℓ p t` |
| `Av12453/TwoThreshold/Kernel.lean:275` | `private def Gaux` — the fuel form of `eq:G` |
| `Av12453/TwoThreshold/Kernel.lean:320` | `G p := Gaux (p.1 + p.2 + 1) p` |
| `Av12453/TwoThreshold/KernelCount.lean:219` | `av12453_count_kernel` itself |

**Reading aids (proved, so *not* part of the trusted base — but these are what to read):**
`Basic.lean:816` `contains_beta_iff` and `:891` `contains_12453_iff` (the index form:
`∃ i₁<i₂<j<k<l` with `w[i₁]<w[i₂]<w[l]<w[j]<w[k]`, i.e. ranks `1,2,4,5,3` in position order
= `12453`); `Basic.lean:663` `beta_two : beta 2 = [0,1,3,4,2]`; `Defs.lean:520` `mem_perms`;
`Kernel.lean:208,220,224,233,330` (`K_zero`, `D_one`, `D_succ`, `K_succ_eq`, `G_eq` — the
paper's displays restated as theorems); `KernelSupport.lean:317,330` (`K_succ_eq_ctrls`,
`G_eq_ctrls` — the same displays over the paper's unrestricted ranges).

**Transcription check performed, `d = 2` (`eq:T`/`eq:U` at tex:1115-1127, `eq:K`/`eq:D`/`eq:G`
at tex:1494-1546, `eq:answer-G` at tex:1554):**

| paper | Lean |
|---|---|
| `T_{0,h}(p) = (h, p₁+p₀−1−h)`, `h < p₀` (only `i = 0`, since `d−2 = 0`) | `(h, p.2 + p.1 - 1 - h)`, `h ∈ range p.1` |
| `U_h(p) = (p₀, h)`, `δ_h = p₁−1−h`, `h < p₁` | `(p.1, h)`, head `ℓ + (p.2 - 1 - h)` |
| `D_1 = 1_{p=t}`, `D_ℓ = 2K_{ℓ−1}` (`ℓ ≥ 2`) | `Kernel.lean:212`, `D_one`, `D_succ` |
| split over `a,b ≥ 1`, `a+b = ℓ−1` | `∑ a ∈ Finset.Ico 1 (ℓ-1)`, `b = ℓ-1-a` (so `a` runs `1 … ℓ-2`) |
| `G_p = 1_{p=0} + ∑_{h<p₀} G_{T_{0,h}(p)} + ∑_{h<p₁} ∑_t K_{δ_h}(U_h(p),t) G_t` | `G_eq`, `Kernel.lean:330` |
| `a_n^{(2)} = G_{(n,0)}` | `G (n, 0)` |

ℕ-subtraction introduces no truncation anywhere: `h < p₀` forces `p₁+p₀−1−h` exact, and
`h < p₁` forces `p₁−1−h` exact. The only deviations from the displays are the two truncated
control sums, closed as in NOTE B.

---

## 4. What I verified and found correct

**Statement, axioms, non-circularity**

* `#check` gives exactly `∀ (n : ℕ), TwoThreshold.G (n, 0) = (OneThreshold.avoiders n (beta 2)).card`
  — no hypotheses, no free variables (`relaxedAutoImplicit = false` in `lakefile.toml`).
* `#print` shows the proof term is
  `fun n => av12453_count_kernel_of (fun _ℓ p L hℓ => H_factor hℓ p L) n` — the
  `TwoThresholdFactorization` hypothesis is discharged, not carried.
* `#print axioms` → `[propext, Classical.choice, Quot.sound]`. Same for
  `av12453_count`, `H_factor`, `K_support`, `G_eq_H`, `av1342_count`,
  `av1342_count_kernel`, `avoids_beta_iff_forall_trigger`.
* Statement-drift check: an independent restatement with fully qualified names
  (`theorem drift : ∀ n : ℕ, Av12453.TwoThreshold.G (n, 0) = (Av12453.OneThreshold.avoiders n (Av12453.beta 2)).card := fun n => …av12453_count_kernel n`)
  elaborates and is axiom-clean.
* **Non-circular.** The value-only (definitional) closure of `TwoThreshold.G` is exactly
  `{G, K, S, ctrls}` (plus the private `Kaux`/`Gaux`) — it contains neither `H` nor
  `avoiders`. The closure of `avoiders` is `{avoiders, perms, Avoids, Contains, OrderIso}`
  plus its `Decidable` instances. The theorem equates two independently defined computable
  quantities. (The `Decidable` instances are proof-carrying, so the filtered `Finset` is
  instance-independent.)
* Exactly one `def` each for `avoiders`, `perms`, `Contains`, `Avoids`, `OrderIso`, `beta`;
  `TwoThreshold.K/D/G/H` are distinct from the `OneThreshold` scalars by namespace.

**Axiom hygiene, verified independently of the project's own tooling**

* The project's sweep: `lake env lean Av12453/Axioms.lean` → **exit 0**,
  `checked 1438 declarations`, 1942 output lines, **zero** `sorryAx`, and the only three
  axiom names appearing anywhere in the output are `Classical.choice`, `Quot.sound`,
  `propext` (1353 declarations use axioms, 85 use none).
* Its enumeration is complete for `constNames`: an independent count over the twenty
  modules gives **1438**, and all twenty module-name arguments match a real module
  (`unmatched module names: []`).
* **Independent, cache-free, body-level closure walk** (does *not* consult the
  olean-recorded axiom tables that `collectAxioms` short-circuits on; matches
  `.thmInfo v => v.value` explicitly — see NOTE H):
  * seeded at `av12453_count_kernel`: **8013** constants visited, axioms exactly
    `[propext, Classical.choice, Quot.sound]`, **zero** reachable unsafe/partial constants;
  * seeded at all **2622** project `constNames` + `extraConstNames` (so the four
    `.match_1.splitter` constants the sweep misses are covered): **11922** constants
    visited, same three axioms, zero unsafe, and the 1180 names absent from the kernel
    environment are all IR-only artefacts (`._closed_N`, `._boxed`, `._lam_N`, `.spec_N`,
    `._redArg`).
* **No `sorry` anywhere.** A strict `\bsorry\b` grep over the twenty modules matches only
  three prose lines describing historical placeholders (`OneThreshold/Defs.lean:1220`,
  `OneThreshold/KernelCount.lean:81`, `TwoThreshold/KernelCount.lean:279`).
* **Zero `native_decide`** in the tree. The only `by decide` on a *named* theorem is
  `beta_one`/`beta_two` (`Basic.lean:660,663`) — kernel `decide`, hence checked.
* Every `set_option` in the tree is soundness-neutral: `maxRecDepth` (elaborator budget) or
  a linter toggle. `lakefile.toml` sets only `pp.unicode.fun`, `relaxedAutoImplicit=false`,
  `weak.linter.mathlibStandardSet`, `maxSynthPendingDepth=3`. No `axiom`, `unsafe`,
  `@[implemented_by]`, `@[extern]`, `opaque`, `partial def`, `macro_rules`, or `elab` in
  the library.
* **Mathlib is pristine at the pinned revision.** `.lake/packages/mathlib` HEAD
  `0df444a360eaa60ab8c11dca51a86af692955474` = tag `v4.33.1` = the `lake-manifest.json`
  rev; `git status --porcelain` empty. Toolchain `leanprover/lean4:v4.33.1`. All eight
  transitive packages at their manifest revisions.
* `lake build` completes successfully (8727 jobs, no errors). All twenty `.olean` files are
  newer than their sources, so the built artifacts match the checked-in Lean.

**Faithfulness to the paper, verified numerically against an independent implementation**

* I wrote a Python transcription of `eq:K`/`eq:D`/`eq:G` **from the `.tex` alone**, with
  **untruncated** control sums over a mass-14 box
  (an auxiliary script outside the repository). Against a Lean dump it
  agrees on **all 600 `K`-entries** (`ℓ ≤ 3`, `‖p‖₁ ≤ 3`, `‖t‖₁ ≤ 4`; 130 nonzero) and
  **all 28 `G`-values** (`‖p‖₁ ≤ 6`) — **zero mismatches**. No nonzero untruncated
  `K`-entry has `‖t‖₁ > ‖p‖₁`, so `S p` loses nothing.
* `lem:support` (`eq:support`) holds on every untruncated row computed: **0 violations**.
* Series: my untruncated implementation gives
  `1, 1, 2, 6, 24, 119, 694, 4581, 33286, 260927, 2174398, 19053058, 174094868`, matching
  `eq:first-terms` (tex:174) and `code/data/av12453_terms_0_150.txt` line for line.
* Lean's `G (n,0)` for `n ≤ 8` is `1,1,2,6,24,119,694,4581,33286` — identical.
* **Independent brute force.** My own enumeration of all `n!` permutations against all
  `C(n,5)` subsequences of the pattern `[0,1,3,4,2]` gives `1,1,2,6,24,119,694,4581,33286`
  for `n ≤ 8`, matching both.
* Lean's `(avoiders n (beta 2)).card` for `n ≤ 7` is `1,1,2,6,24,119,694,4581` — identical
  to my brute force, so `Contains`/`avoiders` denote the right set, not merely the right
  count. Controls: `avoiders n (beta 1)` = `1,1,2,6,23,103,512,2740` = `|Av_n(1342)|`;
  `perms n` = `n!` for `n ≤ 7`; `avoiders n 231` = Catalan.
* `Contains` edge cases behave correctly: `Contains [0,1,3,4,2] (beta 2) = true`,
  `Contains [1,1,0] 231 = false` (repeats cannot realize distinct entries),
  `Contains [0,0,1] [0,0] = true`, `Contains [30,90,10] 231 = true` (scaling-invariant).
* `contains_12453_iff` (`Basic.lean:891`) is a **proved** equivalence with the classical
  index definition, and its rank pattern in position order is `1,2,4,5,3 = 12453`.
* The `eq:K` transcription is faithful term by term (table in §3), checked by hand against
  tex:1115-1127 and tex:1494-1556.
* The fuel is provably adequate: `K_succ_eq` (`Kernel.lean:233`) and `G_eq` (`:330`) hold
  **unconditionally** (only `1 ≤ ℓ`), with no fuel side condition, and `Kaux_congr`/
  `Gaux_congr` prove fuel-independence above the grade. I checked by hand that every
  right-hand-side occurrence has grade `< w`, so `K_zero` + `K_succ_eq` pin `K` as the
  unique solution of a well-founded recurrence.
* The chain is unconditional end to end: `av12453_count_kernel` → `G_eq_H_of` +
  `KernelFactor.H_factor` → `Counting.av12453_count` (itself unconditional, with
  `DeferredHyp`/`CompleteHyp` discharged by `deferredHyp`/`completeHyp`). No Prop-packaged
  hypothesis survives into any statement.
* Cross-check inside the development: `TwoThreshold/Counting.lean:583` is a
  `#guard_msgs`-guarded `#eval` comparing `(avoiders k (beta 2)).card` against `H (k,0) []`
  for `k ≤ 7` — a live regression check that would fail the build if either drifted.

**Non-vacuity**

`G (n,0)` diverges from `n!` at `n = 5` (119 vs 120), so `avoiders` is a genuine proper
subset of `perms`; the split terms with `ℓ ≥ 2` are genuinely exercised in the recursion
below `(n,0)`; and `G`'s closure does not mention `avoiders`, so the equality is not
definitional.

---

## 5. Plain-language verdict for the author

The certificate is solid. I tried hard to break it and could not: the headline theorem is
unconditional, uses only Lean's three standard axioms (verified twice, the second time by a
walk over the actual proof bodies that ignores the axiom tables baked into the `.olean`
files), contains no `sorry` or `native_decide`, sits on an unmodified Mathlib at the pinned
`v4.33.1`, and equates two objects that are defined independently of each other — so it is
not circular and not trivially true. Every definition in its statement means what it claims:
`avoiders n (beta 2)` really is the set of `12453`-avoiding permutations (I re-enumerated
them by brute force and got the same sets and the same counts `1,1,2,6,24,119,694,4581`),
and `G` really is `eq:K`/`eq:D`/`eq:G` (I re-implemented those from the `.tex` with the
paper's *unrestricted* sums and matched every Lean value). Nothing I found threatens the
mathematics. What I would change is the surrounding prose. `formal/README.md:14` says the
theorem is "the correctness statement of the paper's polynomial-time algorithm for 12453";
it is the correctness statement of the *unreduced* `d = 2` recurrence, which the paper
itself calls the `O(N^8)`/`O(N^5)` version — the advertised `O(N^7)`/`O(N^4)`, and the
program that actually produced the published 150- and 300-term data, both go through the
translation quotients of §7, none of which is formalized, and no complexity claim is
formalized at all. Two smaller process items: the sweep does not in fact cover `example`
declarations (so the frozen-interface drift checks and the numeric `decide`s are unguarded),
and nothing automated runs it, since CI only does `lake build` and a `sorry` is merely a
warning there. Add the sweep to CI, fix the three overstatements (README:14, the `example`
claim, and §9.2's placement of `Trigger.lean`/`FirstLetter.lean` in the chain when they are
in fact used by nothing), ship a LICENSE file, add "with `a,b ≥ 1`" to the kernel-phase
paragraph in `sec:algorithm`, and cite the certificate somewhere in the manuscript — which
currently never mentions it at all.
